This is a very easy problem.

Optimal Sol: TC- O(N)  SC- O(1)
     vector<int> runningSum(vector<int>& nums) {
        int n=nums.size();
        int sum=0;
        vector<int> ans;
        
        for(int i=0;i<n;i++)
        {
            sum+=nums[i];
            ans.push_back(sum);
        }
        return ans;
    }
};
